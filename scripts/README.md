# Initial Conditions

To generate the initial conditions, run `init_model.py` and pass the model vertical resolution as an argument.

Valid vertical resolution values are:

- `2`
- `4`
- `8`

Example:

```bash
python init_model.py 2
```
